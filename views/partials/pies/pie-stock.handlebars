<li>
    <h2>{{pie_name}}</h2>

    {{!-- <button class="delete-pie" data-id="{{id}}" data-id="{{this.id}}">
        <i class="fas fa-times"></i>
    </button> --}}

    <button class="change-stock" data-id="{{id}}" data-newstock="{{stock}}">
		{{#if stock}}<i class="fas fa-times"></i>{{else}}<i class="fas fa-plus"></i>{{/if}}
	</button>

    <button class="delete-pie" data-id="{{this.id}}">
            <i class="far fa-trash-alt"></i>
        </button>
    </button>
</li>
<p>{{description}}</p>